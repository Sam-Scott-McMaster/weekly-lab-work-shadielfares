#Answers to questions
#Shadi El-Fares
#Sept.30.24

a. The script di, takes in the first input $1, being ls and outputs all unhidden files to stdout.

b. The sequence of commands sens to stdout "a 1 a1C 1*10". This was my initial guess as although I thought twice about D, everything is treated like a string in bash.
